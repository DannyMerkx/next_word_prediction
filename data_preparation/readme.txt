Scripts for preparing the data.

prep_next_word.py prepares the ENCOW data for language model training. Run before training.

prep_exp_data.py prepares the human reading data for LMER fitting.
load_model.py uses the trained Language models to extract surprisal from the human reading data.
Run both before LMER fitting.
